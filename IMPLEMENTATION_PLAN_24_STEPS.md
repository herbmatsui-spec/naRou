# naRou: Masterpiece Edition - 24ステップ実装計画書

## 概要
この計画書は、コードレビューで特定された問題を解決するための24段階の詳細な実装計画です。各ステップは具体的かつ達成可能なタスクに分割されており、段階的に改善を進めることができます。

## ステップグループ分け

### フェーズ1: 基盤修復 (Steps 1-6)
緊急度が高く、システムの基本機能を復旧させるステップ

### フェーズ2: アーキテクチャ改善 (Steps 7-12)
God Object問題の解決と疎結合化の推進

### フェーズ3: コード品質向上 (Steps 13-18)
保守性と可読性の向上

### フェーズ4: パフォーマンス最適化 (Steps 19-21)
実行効率の改善

### フェーズ5: テスト・セキュリティ強化 (Steps 22-24)
信頼性と安全性の確保

---

## フェーズ1: 基盤修復 (Steps 1-6)

### Step 1: 循環インポート修復
**ファイル**: `ranking_manager.py`
**変更点**: ファイル末尾に `RANKING_MANAGER = RankingManager()` を追加
**目的**: テスト収集を復旧させる
**検証**: `python -m pytest tests/test_architecture_refactor.py -v` がパスする

### Step 2: WebSocket認証基盤実装
**ファイル**: `web_server.py`
**変更点**:
- 基本的なOriginチェックミドルウェア追加
- レート制限のスケルトン実装
**目的**: 基本的なセキュリティ対策
**検証**: ローカルホストからのアクセスは許可、異なるOriginからはブロック

### Step 3: 重複初期化除去 (Engine.__init__)
**ファイル**: `game.py` (lines 66-74)
**変更点**: 重複する設定マネージャー初期化を削除
**検証**: ゲーム起動時に設定が正しく読み込まれることを確認

### Step 4: 未使用インポート削除
**ファイル**: 全プロジェクトファイル
**変更点**:
- `game.py` から未使用の `from sound_manager import SoundManager` 等を削除
- 同様に他ファイルも対応
**目的**: 依存関係の明確化
**検証**: `ruff check --select F401 .` がクリーンになる

### Step 5: 定数化準備
**ファイル**: `constants.py`
**変更点**:
- 魔法数字用の名前付き定数グループを追加
  ```python
  # ゲームバランス定数
  SPAWN_SNAIL_CHANCE = 0.35
  SPAWN_MONSTER_CHANCE = 0.85
  SPAWN_ITEM_CHANCE = 0.7
  AUTO_SAVE_INTERVAL = 50
  TITLE_CHECK_INTERVAL = 10
  ```
**目的**: マジックナンバーの一元管理
**検証**: 定数が適切に名前付けされていること

### Step 6: 基本的なテストスイート復旧
**ファイル**:
**変更点**:
- `pytest.ini` に `filterwarnings` 設定追加
- テスト実行スクリプトの更新
**目的**: CI/CDの基盤作り
**検証**: `python -m pytest` がエラーなく実行開始する

---

## フェーズ2: アーキテクチャ改善 (Steps 7-12)

### Step 7: GameState クラス抽出 (Part 1)
**ファイル**: `game_state.py` (新規作成)
**変更点**:
- `Engine` クラスからゲーム状態関連のプロパティを抽出
  - プレイヤー、ペット、エンティティリスト
  - ゲームマップ、ダンジョンレベル
  - インベントリ、ステータス効果
**目的**: 責務の分離開始
**検証**: 新しいクラスがインスタンス化できること

### Step 8: GameState クラス抽出 (Part 2)
**ファイル**: `game.py`, `game_state.py`
**変更点**:
- `Engine.__init__` で `GameState` オブジェクトを作成し使用
- 状態アクセスメソッドの移植
**目的**: 状態管理の分離
**検証**: ゲームが正常に起動・プレイできること

### Step 9: SystemCoordinator 抽出
**ファイル**: `system_coordinator.py` (新規作成)
**変更点**:
- `SystemManager` のラッパーとして機能
- システム間の依存性解決を担当
- イベントバスとの連携
**目的**: システム調達の中間層
**検証**: 全システムが正常に登録・初期化されること

### Step 10: エンティティ管理の委譲
**ファイル**: `entity_manager.py` (新規作成)
**変更点**:
- エンティティの生成、取得、削除ロジックを移譲
- スペーシャルインデックス（グリッド）の実装準備
**目的**: エンティティ管理の専門化
**検証**: エンティティ操作が正常に機能すること

### Step 11: 描画システムの完全分離
**ファイル**: `render_system.py` 等（既存）
**変更点**:
- `Engine.render()` メソッドを `RenderSystem` に完全移譲
- レンダラーオブジェクトの生成責務を移譲
**目的**: 描画ロジックの独立
**検証**: ゲームが正常に描画されること

### Step 12: 入力システムの独立化
**ファイル**: `input_handler.py` 等（既存）
**変更点**:
- `Engine` への依存を最小化（必要なインターフェースのみ）
- コールバックベースのイベント配信へ移行準備
**目的**: 入力処理の疎結合化
**検証**: キー入力が正常に処理されること

---

## フェーズ3: コード品質向上 (Steps 13-18)

### Step 13: 関数サイズ削減 - advance_world
**ファイル**: `game.py`
**変更点**:
- `advance_world()` メソッドを小さな関数に分割
  - `_process_entity_turns()`
  - `_process_status_effects()`
  - `_process_food_rot()`
  - `_process_survival()`
  - `_process_natural_healing()`
  - `_process_world_events()`
  - `_process_title_checks()`
  - `_process_job_exp()`
  - `_process_skill_tree_checks()`
  - `_process_guild_quest_reset()`
  - `_process_faction_influence()`
  - `_process_pet_bonding()`
  - `_process_fx_updates()`
**目的**: 単一責務原則の遵守
**検証**: 各関数が50行以内に収まること

### Step 14: 一貫した命名規則適用
**ファイル**: 全プロジェクトファイル
**変更点**:
- ブール関数: `is_*` または `has_*` で統一
- 定数: `UPPER_CASE` に統一
- 変数・関数: `snake_case` に統一
**目的**: PEP 8 準拠と可読性向上
**検証**: `ruff check --select N802,N803,N806 .` が改善する

### Step 15: ドキュメント文字列統一
**ファイル**: 全プロジェクトファイル
**変更点**:
- 全クラス・関数に適切な docstring を追加
- NumPy または Google スタイルに統一
- タイプヒントと相互参照の明確化
**目的**: IDEサポートとドキュメント生成の向上
**検証**: `pydocstyle` がエラーなく通ること

### Step 16: エラーハンドリングの統一
**ファイル**: 全プロジェクトファイル
**変更点**:
- ベース例外クラスの統一使用
- エラーメッセージのフォーマット統一
- ロギングレベルの適切な使用 (DEBUG, INFO, WARNING, ERROR)
**目的**: デバッグと監視の容易化
**検証**: 例外発生時のログフォーマットが一貫していること

### Step 17: コメントの質改善
**ファイル**: 全プロジェクトファイル
**変更点**:
- `TODO`、`FIXME`、`HACK` コメントを Issue に変換または実装
- 価値のないコメント（「これはxを設定する」）を削除
- 重要なアルゴリズムや設計決定の説明コメントを追加
**目的**: コードの自己文書化
**検証**: 未対応の `TODO` がなくなること

### Step 18: タイプヒントの徹底
**ファイル**: 全プロジェクトファイル
**変更点**:
- 未注釈の関数・変数にタイプヒント追加
- `Any` の使用を最小化
- ジェネリクスと Protocol の適切な使用
**目的**: 静的解析の効果向上
**検証**: `mypy .` がエラーなく通ること

---

## フェーズ4: パフォーマンス最適化 (Steps 19-21)

### Step 19: 空間インデックスによるエンティティ検索最適化
**ファイル**: `entity_manager.py` (Step 10で作成)
**変更点**:
- グリッドベースの空間パーティショニング実装
- `get_entity_at()`、`get_entities_in_range()` の最適化
- 動的オブジェクトプールによるメモリ割り当て削減
**目的**: O(n) → O(1) 平均ケースでのエンティティ検索
**検証**: 1000エンティティ時の検索速度が10倍以上改善

### Step 20: ダーティフラグによる演算削減
**ファイル**: `game_state.py` 等
**変更点**:
- FOV 計算: プレイヤー位置変更時のみ再計算
- 経路探索: ターゲットまたは障害物変更時のみ再計算
- システム更新: 関連コンポーネント変更時のみ実行
**目的**: 不要な演算の削減
**検証**: 同等のゲームプレイでCPU使用率が30%削減

### Step 21: シリアライゼーション最適化
**ファイル**: `save_system.py`
**変更点**:
- `pickle` から `msgpack` または `orjson` へ移行
- 差分セーブの実装（前回セーブからの変更分のみ保存）
- 非同期バックアップ処理
**目的**: セーブ/ロード時間の短縮とファイルサイズ削減
**検証**: セーブ時間が50%削減、ファイルサイズが60%削減

---

## フェーズ5: テスト・セキュリティ強化 (Steps 22-24)

### Step 22: セキュリティ基盤強化
**ファイル**: `save_system.py`, `web_server.py`
**変更点**:
- `pickle` の完全除去と `json` スキーマバリデーション実装
- WebSocket接続のOrigin検証とレート制限実装
- 入力サニタイズの徹底（特に願いシステム）
**目的**: 情報漏洩とリモートコード実行リスクの除去
**検証**: セキュリティスキャンツール（bandit）がクリーンになる

### Step 23: 包括的テストスイート構築
**ファイル**: `tests/` ディレクトリ
**変更点**:
- モック/スタブフレームワークの確立 (`conftest.py`)
- 主要システム（戦闘、アイテム、セーブロード）の単体テスト
- 統合テストシナリオの作成（ダンジョン探索、転生、ウェブ連携）
- カバレッジ目標: 80% 以上
**目的**: リグレッション防止とリファクタリングの安全性確保
**検証**: `python -m pytest --cov=./ --cov-report=html` で 80%+ カバレッジ達成

### Step 24: CI/CD パイプライン構築
**ファイル**: `.github/workflows/ci.yml` (新規作成)
**変更点**:
- Python 3.9-3.12 のマルチバージョンテスト
- セキュリティスキャン (bandit, safety)
- パフォーマンスベンチマークの基線測定
- ドキュメントビルドとリンクチェック
- リリース時の自動バージョンタグ付け
**目的**: 品質ゲートの自動化とリリースプロセスの標準化
**検証**: プッシュ時に全チェックがパスし、失敗時はブロックされる

---

## 実装ガイドライン

### 作業フロー
1. 各ステップごとにIssueを作成し、詳細を記述
2. ステップ開始前に該当するテストを書く（TDD推奨）
3. 小さなコミットで頻繁にプッシュ
4. 各ステップ完了後に関連テストを実行し、 regression を確認
5. 完了したらドキュメントを更新

### 品質ゲート
- 各ステップ終了時: `ruff check . && mypy .` がクリーン
- セキュリティ関連ステップ: `bandit -r .` がクリーン
- パフォーマンス関連ステップ: ベンチマークスコアが改善
- アーキテクチャ関連ステップ: 循環インポートチェックツールがクリーン

### リスク軽減
- 大きなリファクタリングは機能フラグまたは段階的移行で実施
- 各ステップ完了後にプレイテストを実施し、ゲームバランスに変更がないことを確認
- 緊急修正が必要な場合は、この計画から一時的に逸脱しても構わないが、理由を記録

---

## 成功基準

この24ステップを完了すると、以下の状態を達成します：

1. **ビルド & テスト**: `python -m pytest` が 90% 以上のパス率で実行可能
2. **セキュリティ**: 外部からの脅威に対する基本的な防御が実装済み
3. **保守性**: 新規開発者が 1 日でコードベースの構造を理解可能
4. **パフォーマンス**: 中規模ゲームセッション（1000ターン）で 60FPS 以上安定
5. **拡張性**: 新しいシステムを追加する際に既存コードへの影響が最小限
6. **信頼性**: セーブ/ロードの互換性が複数バージョンにわたって保証

この計画は段階的に価値を提供するように設計されており、どのステップからでも開始して価値を得ることができますが、順序通りに実行することで最も効率的に改善を進めることができます。

---
*この計画書はnaRou: Masterpiece Editionの品質向上と長期保守性確保を目的としています。*
