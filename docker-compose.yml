version: '3.8'
services:
  narou:
    build: .
    command: python game.py
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - CONFIG_ENCRYPTION_KEY=${CONFIG_ENCRYPTION_KEY}
