# ライティング・ポストプロセス・パーティクルシステム 実装計画書

Canvas/WebGLレイヤー分離：背景・タイル・エンティティ・エフェクト・UI。動的ライト（松明・魔法・時間帯）、ブルーム、スクリーンシェイク、パーティクル（火花・魔法陣・血しぶき）を demos/ 共通ライブラリ化

静止画から「生きた映像」へ。没入感・インパクト劇的向上

## 概要

本ドキュメントは、既存のCanvas 2DベースのウェブクライアントをWebGLベースのレイヤー分離レンダリングに移行し、動的ライティング、ブルーム、スクリーンシェイク、高度なパーティクルシステムを実装するための詳細な計画を示す。これらの機能を demos/ ディレクトリ内の共通ライブラリとして提供し、ゲームクライアントおよびデモシーンで再利用可能とする。

対象期間：概算 6週間
総ステップ数：36ステップ（小さな、検証可能な単位に分割）
想定文字数：約10,000字

## 前提条件

- Python バックエンドゲームサーバー（game.py, web_server.py）は変更せず、既存の JSON API を利用
- フロントエンドは web_game_client.html をベースに改修
- WebGL ライブラリとして PixiJS v7 を CDN から利用（軽量かつ豊富なフィルターエフェクト）
- デモ・共通ライブラリは demos/lib/ 配下に配置し、ESM モジュールとして提供

## 実装ステップ（36ステップ）

### Phase 0: 準備と基盤構築

**ステップ 0-1: 開発環境の準備**
- PixiJS v7 を CDN (https://cdn.pixijs.com/pixi.js) から読み込む設定を web_game_client.html に追加
- ローカル開発用に demos/lib/ ディレクトリを作成し、共通ライブラリの置き場を準備
- Git ブランチ `feature/webgl-postprocess` を作成し、変更を隔離

**ステップ 0-2: 既存レンダリングパイプラインの解析**
- web_game_client.html の Canvas 2D レンダリング関数 (`render()`) を詳細に分解
- 描画順序とデータフローを図解：マップ → タイル → アイテム → エンティティ → 浮遊テキスト → パーティクル → HUD
- サーバーから送信される JSON 状態の構造を確認し、どのデータが新しいレンダリングに必要かを抽出

**ステップ 0-3: 基本的な PixiJS アプリケーションの雛形作成**
- web_game_client.html 内の `<canvas id="gameCanvas">` を PixiJS の Renderer にバインド
- PixiJS Application オブジェクトを初期化し、背景色を黒に設定
- リサイズ処理を追加し、ウィンドウサイズ変更に対応（既存の Canvas サイズロジックを流用）

**ステップ 0-4: テクスチャアトラスの概念設計**
- フォントグリフ（英数字・絵文字・シンボル）を 1 つのテクスチャアトラスに packing する方針を定める
- 既存の描画文字（@, p, #, ., >, ⛩️, アイテム文字等）を SVG またはビットマップで事前生成し、アトラス作成ツールを検討（後工程で実装）

### Phase 1: レイヤー分離の実装

**ステップ 1-1: 背景レイヤーの作成**
- PixiJS Container オブジェクト `backgroundLayer` を作成し、ステージの最背面に追加
- 初期状態では単色背景（濃紫）を塗りつぶす Sprite で埋める

**ステップ 1-2: タイルレイヤーの作成**
- PixiJS Container オブジェクト `tileLayer` を作成し、背景レイヤーの次に追加
- タイルデータ（マップの visible_tiles）を受け取り、各タイルを Sprite または TilingSprite で表現する仮実装を行う
- まずはデバッグ用に色付きの矩形でタイルを描画し、座標変換が正しいことを確認

**ステップ 1-3: エンティティレイヤーの作成**
- PixiJS Container オブジェクト `entityLayer` を作成し、タイルレイヤーの次に追加
- エンティティデータ（プレイヤー、ペット、モンスター）を受け取り、各エンティティを Sprite で表現する仮実装
- プレイヤーは `@`、ペットは `p`、モンスターは敵種別ごとに異なる色の矩形で描画

**ステップ 1-4: エフェクトレイヤーの作成**
- PixiJS Container オブジェクト `effectLayer` を作成し、エンティティレイヤーの次に追加
- このレイヤーには後でパーティクルシステムやライティンググローを追加する予定

**ステップ 1-5: UI レイヤーの構成**
- 既存の HTML オーバーレイ（HUD、サイドバー、ログ等）は DOM ベースのまま保持し、PixiJS ステージの上に重ねる
- 必要に応じて、PixiJS で描画する UI コンポーネント（ミニマップ等）をこのレイヤーに移行することを検討

**ステップ 1-6: レイヤー構成頻度の確定とテスト**
- ステージへの追加順序を `backgroundLayer` → `tileLayer` → `entityLayer` → `effectLayer` と確定
- 各レイヤーにダミーデータを流し込み、正しく重ね合わせられているかをブラウザで確認
- デベロッパーツールで PixiJS のインスペクターを使用し、シーングラフを点検

### Phase 2: タイルおよびエンティティのテクスチャレンダリング

**ステップ 2-1: フォントテクスチャアトラスの生成（ツール編）**
- demos/lib/ に `generate_font_atlas.js` を作成し、Node.js と canvas モジュールを使用（またはブラウザベースのオフスクリーンキャンバス）でアトラスを生成
- 対象文字：`@ p # . > ⛩️` およびアイテム文字（% ? $ * ! etc.）と数字・アルファベット
- アトラスサイズを 512x512 とし、1 グリフあたり 24x24 ピクセル（既存の TILE_SIZE）で配置
- 生成されたアトラス画像と JSON メタデータ（グリフ→UV マッピング）を demos/assets/ に保存

**ステップ 2-2: テクスチャアトラスローダーの作成**
- demos/lib/ に `TextureAtlas.js` モジュールを作成し、アトラス画像とメタデータを読み込んでグリフ描画用の Texture を提供
- PixiJS の Texture からサブテクスチャを切り出す機能を実装（または TextureAtlas フレームを使用）

**ステップ 2-3: タイルのテクスチャレンダリング実装**
- タイルレイヤーで、マップの各タイルに対応するグリフ（壁は `#`, 床は `.`, 階段は `>` 等）をテクスチャアトラスから取得し、Sprite で描画
- タイルの走査は既存の Canvas 2D と同じループを流用し、座標を PixiJS の位置に変換
- 初期状態ではグリフの色は白とし、後でライティングによって色調補正を行う

**ステップ 2-4: エンティティのテクスチャレンダリング実装**
- エンティティレイヤーで、プレイヤーは `@`, ペットは `p`, モンスターは種別に応じた文字（例: ガブリは `G`, スライムは `S`）をテクスチャアトラスから取得
- エンティティの色はサーバーから送信される色情報（JSON の entities[].color）を使用して、Sprite の tint を設定
-  size は TILE_SIZE に合わせてスケーリング（PixiJS の Sprite はデフォルトでテクスチャサイズ）

**ステップ 2-5: アイテムのテクスチャレンダリング実装**
- アイテムデータを受け取り、アイテムレイヤー（またはエンティティレイヤーに統合）でアイテム文字をテクスチャアトラスから取得し、Sprite で描画
- 色はアイテムの color プロパティを使用

**ステップ 2-6: テクスチャレンダリングの最適化とバッチング**
- PixiJS のバッチングを有効にするため、全ての Sprite が同じテクスチャアトラスを参照していることを確認
- 必要に応じて、StaticBatch または Mesh ベースのレンダリングに移行する検討を行う（今回は Sprite で十分と判断）

### Phase 3: 動的ライティングシステム

**ステップ 3-1: サーバー側ライティングデータの拡張**
- web_server.py の `_serialize_engine_state` 関数を修正し、動的ライト源のリストを追加
- プレイヤーの持つ光源（デフォルトの杖光）に加えて、マップ上の松明（固定位置）、魔法効果（一時的）、時間帯による環境光色変化を追加
- 各ライト源は {x, y, radius, color, intensity, type, flicker速度} などのプロパティを持つ

**ステップ 3-2: クライアント側ライティングデータの受領と解釈**
- web_game_client.html の状態更新関数で、ライト源リストを取得し、内部状態に保存
- ライト源のタイプ別に処理を分岐： torch（点光源、フリッカー）、 magic（点光源、パルス）、 ambient（全体色補正）

**ステップ 3-3: ライトマッピングの実装（シェーダーまたはスプライトベース）**
- シンプルなアプローチとして、ライト源をエフェクトレイヤーに淡い円形のグロー Sprite で表現する
- 各ライト源に対して、半透明の円形グラフィックを作成し、加算ブレンドで描画
- ライトの強度と色を Sprite の alpha と tint で制御

**ステップ 3-4: 松明のフリッカーアニメーション**
- 松明タイプのライト源に対して、ランダムノイズまたはサイン波による強度変動を追加
- フリッカー周期は 0.1〜0.3秒程度で、見た目が不規則になるように実装
- エフェクトレイヤーの松明 Sprite 毎に独立したタイマーを持たせる

**ステップ 3-5: 魔法効果のパルスアニメーション**
- 魔法タイプのライト源に対して、サイン波による規則的なパルス（拡張・収縮）を追加
- パルス周期は 1.0〜2.0秒程度で、詠唱中の魔法円盤を模倣
- パルスはスケール変化で実装し、色も白みがかったものに変化させる

**ステップ 3-6: 時間帯による環境光色変化の実装**
- サーバーから時間帯情報（0.0〜1.0 の正規化値または「朝」「昼」「夕」「夜」）を受信
- エフェクトレイヤー全体に適用するカラーフィルター（または背景レイヤーの色変化）で、朝は青みがかった白、昼は白、夕はオレンジ、夜は深い青に補正
- 時間帯の変化は滑らかに補間（リニアまたはサイン補間）を行う

**ステップ 3-7: ライトによるタイル・エンティティの色調補正（オプション）**
- より高度な効果として、ライトによる実際の光照射をシミュレートするため、タイルやエンティティの色をライト距離に基づいて乗算
- フラグメントシェーダーを使用するか、またはライトマップを別途レンダリングして乗算合成する
- 初期段階では省略し、グローライトによる間接的な表現で代替（後工程で拡張可能）

### Phase 4: パーティクルシステムの実装

**ステップ 4-1: 共通パーティクルシステムライブラリの設計方針**
- demos/lib/ に `ParticleSystem.js` モジュールを作成し、Emitter と Particle クラスを提供
- パーティクルは位置、速度、寿命、色、透明度、スケール、テクスチャ（またはグリフ）を持つ
- イミッテッドモードとストアドモードのハイブリッドアプローチで、頻繁に生成・破棄されるパーティクルでも GC  pressãoを軽減

**ステップ 4-2: 基本パーティクルクラスの実装**
- `Particle` クラスを作成し、更新メソッドで寿命を減少し、速度に従って位置を更新、アルファとスケールをフェーズに応じて変化させる（フェードイン/アウト、スケールアップ/ダウン）
- 描画は PixiJS の Sprite または Mesh を使用し、テクスチャアトラスからグリフを取得して表現（* で火花、○ で魔法陣、● で血しぶき等）

**ステップ 4-3: エミッタークラスの実装**
- `Emitter` クラスを作成し、発生位置、発生率、速度範囲、寿命範囲、色範囲などのパラメータを持つ
- 毎フレーム、指定された数のパーティクルを生成し、内部プールで管理
- プールサイズを上限とし、枯渇時は古いパーティクルを再利用

**ステップ 4-4: 火花パーティクルエミッターの実装（武器ヒット時）**
- 近接攻撃や遠隔攻撃ヒット時に、ヒット位置から放射状に火花を飛ばすエミッターを作成
- 速度：ランダムな方向と大きさ（0〜2 タイル/秒）、色：黄白〜赤橙、寿命：0.2〜0.5秒、スケール：小さめ
- テクスチャアスタリスク（*）または小さな円を使用

**ステップ 4-5: 魔法陣パーティクルエミッターの実装（詠唱時）**
- 魔法詠唱開始時に、詠唱者の足元に円形の魔法陣を表現するエミッターを作成
- 速度：ほぼゼロ（浮遊感を出すためにわずかな上下漂い）、色：青紫〜白、寿命：詠唱時間に合わせて設定（2〜5秒）
- テクスチャ：○ または輪郭のある円を使用し、アルファでフェードイン/アウト

**ステップ 4-6: 血しぶきパーティクルエミッターの実装（致命傷時）**
- エンティティが死亡したときに、死亡位置から放物線状に血しぶきを飛ばすエミッターを作成
- 速度：上方向に初速がある放物線、色：濃赤〜黒、寿命：0.5〜1.0秒、スケール：中程度
- テクスチャ：● または不規則な滴を使用

**ステップ 4-7: パーティクルシステムをエフェクトレイヤーに統合**
- エフェクトレイヤー内に、すべてのエミッターを管理するマネージャーオブジェクトを作成
- ゲームクライアントの状態更新時に、新しいイベント（攻撃ヒット、魔法詠唱、死亡等）を検知し、対応するエミッターをエフェクトレイヤーに追加
- エフェクトレイヤーは毎フレーム、すべてのエミッターを更新し、死んだパーティクルはプールに戻す

**ステップ 4-8: パーティクルのテクスチャレンダリング最適化**
- 全てのパーティクルSprite が同じテクスチャアトラス（フォントアトラスまたは別途作成したパーティクル専用アトラス）を参照していることを確認
- 必要に応じて、パーティクル用の別テクスチャアトラスを作成し、読み込む

### Phase 5: ポストプロセッシング効果

**ステップ 5-1: レンダーテクスチャ（フレームバッファ）の導入**
- PixiJS の RenderTexture を使用し、すべてのレイヤー（背景→タイル→エンティティ→エフェクト）をオフスクリーンにレンダリング
- レンダーテクスチャのサイズはキャンバスサイズ（960x576）とし、ピクセル比率を考慮した高解像度バッファも検討

**ステップ 5-2: ブルーム効果の実装準備**
- ブルームは以下のパスで構成される：1. 輝度抽出（閾値以上のピクセルを抽出）、2. 横方向ブラー、3. 縦方向ブラー、4. 元のシーンに加算合成
- PixiJS の Filter システムを使用するか、カスタムフィルターを実装する
- demos/lib/ に `BloomFilter.js` を作成し、PixiJS の AbstractFilter を継承

**ステップ 5-3: 輝度抽出パスの実装**
- 輝度抽出フィルターを作成し、輝度 = (R*0.299 + G*0.587 + B*0.114) が閾値（例: 0.8）以上のピクセルを白に、それ以下を黒に変換
- 結果はグレースケールテクスチャとして出力し、アルファチャンネルを使用して後のブラーで活用

**ステップ 5-4: ブラー パスの実装（横方向と縦方向）**
- 単純なガウシアンブラーをシェーダーで実装し、横方向と縦方向の2パスに分離
- カーネルサイズは 9〜15 ピクセル程度とし、パフォーマンスと品質のバランスを取る
- 2つのフィルターインスタンスを作成し、順番に適用

**ステップ 5-5: ブルームの合成と強度調整**
- ブラー結果を元のレンダーテクスチャに加算ブレンドで合成し、強度パラメータ（0.0〜2.0）で調整
- 強度は設定可能とし、デフォルトは 0.5 とする
- ブルーム対象は魔法光源、松明、希少アイテムのオーラ等、明るい部分に限定するため、輝度抽出の閾値を調整

**ステップ 5-6: スクリーンシェイクの実装**
- スクリーンシェイクは、レンダーテクスチャ全体またはステージの位置をランダムにオフセットさせる効果
- シェイク強度と持続時間を状態から受信し、経過時間に従って指数関数的に減衰させる
- オフセットは X と Y 軸それぞれに独立したランダム値（例: ±4 ピクセル）を適用し、毎フレーム新しい乱数を生成

**ステップ 5-7: エフェクト適用順序の最適化**
- 最終的なレンダリングパイプラインは以下の通り：
  1. 各レイヤーをオフスクリーンレンダーテクスチャに描画（レイヤー分離済み）
  2. エフェクトレイヤー（ライトグロー、パーティクル）を合成
  3. ブルームフィルターを適用（輝度抽出→横ブラー→縦ブラー→加算）
  4. スクリーンシェイクを適用（ステージ全体の位置オフセット）
  5. 結果をキャンバスに出力
- ただし、スクリーンシェイクはブルーム前でも後でもよいが、通常はブルーム前の方が自然（光のブレがシェイクされる）

**ステップ 5-8: エフェクトのオン/オフ切り替え機能**
- パフォーマンス調査や互換性のため、ブルームとスクリーンシェイクを設定でオン/オフできるようにする
- URL パラメータまたはゲーム内設定メニューからトグル可能とし、デフォルトはオン

### Phase 6: 統合、最適化、デモ作成

**ステップ 6-1: ゲームサーバーとの連携テスト**
- ローカルでゲームサーバー（python game.py）とウェブサーバー（web_server.py）を起動し、ウェブクライアントが正常に状態を取得し、レンダリングできるかを確認
- 基本的なプレイ（移動、攻撃、魔法使用）がビジュアルエラーなく行えるかをテスト

**ステップ 6-2: パフォーマンスプロファイリングと最適化**
- ブラウザのデベロッパーツールでフレームレートと描画コール数を監視
- テクスチャのバインド数を減らすため、可能な限り同じテクスチャアトラスを参照させる
- 不可視のエンティティやアイテムはレンダリングから除外（カリング）を行う

**ステップ 6-3: アニメーションタイルのサポート（水、松明、魔法陣）**
- アニメーションタイル用に、テクスチャアトラスからスプライトストリップ（横方向にフレームが並んだもの）を使用するか、または AnimatedSprite を実装
- 水タイル：波紋アニメーション（フレーム4〜8周期）、松明：炎アニメーション（フレーム4〜6周期）、魔法陣：回転またはパルスアニメーション
- アニメーションはタイムデルタに基づいてフレームを進める

**ステップ 6-4: パララックス背景レイヤーの追加**
- 背景レイヤーにパララックス効果を追加し、遠景（星空や遠くの山）がゆっくりとスクロールするようにする
- パララックス率は 0.1〜0.3 とし、プレイヤーのカメラ移動に従ってゆっくりと動かす
- 遠景は大きなテクスチャ（または繰り返しパターン）を使用し、オフセットを適用

**ステップ 6-5: UI のフェードイン/アウトとトランジション**
- インベントリ画面やステータス画面などのモーダルウィンドウに、フェードイン/アウトまたはスライドイン/アウトのアニメーションを追加
- PixiJS の Tween ライブラリ（または自作の補間関数）を使用し、アルファと位置を滑らかに変化させる

**ステップ 6-6: デモファイルの作成（demos/ ディレクトリ内）**
- `demos/particle_demo.html`：火花、魔法陣、血しぶきのパーティクルシステムを個別にデモンストレーション
- `demos/lighting_demo.html`：松明のフリッカー、魔法のパルス、時間帯による環境光変化をデモンストレーション
- `demos/bloom_shake_demo.html`：ブルームとスクリーンシェイクの効果を組み合わせてデモンストレーション
- 各デモは共通ライブラリ（demos/lib/）をインポートし、スタンドアローンで動作するものとする

**ステップ 6-7: ドキュメントとコードコメントの整備**
- 共通ライブラリの各モジュールに JSDoc スタイルのコメントを追加し、使い方を説明
- web_game_client.html の変更点をコメントで明記し、将来のメンテナンスを容易にする
- demos/README.md を作成し、各デモの概要と実行方法を説明

**ステップ 6-8: 最終統合テストと品質確認**
- すべての機能を組み合わせたシナリオ（魔法戦闘での血しぶきと魔法陣とブルームとスクリーンシェイク）をプレイテスト
- バックエンドのログやエラーが出ないかを確認し、フレームレートが 30 FPS 以上を維持するかを測定
- 必要に応じてパラメータ（パーティクル数、ブラー強度等）を調整

**ステップ 6-9: リリース準備とクリーンアップ**
- 一時的なデバッグコードやコンソールログを削除またはコメントアウト
- ファイルサイズを最小化し、可能な限り CDN からの依存度を高める（ローカルファイルは最小限に）
- リポジトリのメインブランチにマージする準備を行い、プルリクエストの説明を作成
